# RAG Architecture

Project/User Context → Query Construction → Metadata & Permission Filters → Hybrid Retrieval → Re-ranking → Context Assembly → AI Reasoning → Cited Answer/Recommendation.

Use vector/semantic and keyword retrieval where useful. Never include unauthorized or irrelevant knowledge in model context.

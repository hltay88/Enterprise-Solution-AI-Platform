# AI Provider Abstraction

Provider-neutral interfaces: chat/reasoning, embeddings, reranking and structured output.

Application code depends on interfaces, not vendor SDKs. Record provider, model, configuration, token limits, timeout/retry policy and cost metadata.

# Phase 5 Security Test Plan

Test tenant isolation, authorization bypass, prompt injection, malicious documents, data exfiltration, insecure tool use, excessive agent permissions, audit tampering, secret leakage, unauthorized retrieval, file upload abuse and API abuse/rate limits.

Security is a release gate.

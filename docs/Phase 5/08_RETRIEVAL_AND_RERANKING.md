# Retrieval and Re-ranking

Stages: normalize query; apply tenant/project/domain/permission filters; retrieve candidates; re-rank; apply diversity/freshness; build context; return sources.

Record query, filters, retrieved IDs, scores, model/reranker and timestamp.

Measure recall, precision, citation coverage, latency and unsupported-answer rate.

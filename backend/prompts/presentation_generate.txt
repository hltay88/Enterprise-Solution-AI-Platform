Generate a structured executive presentation from an immutable Atlas source snapshot.

Rules:
- Use ONLY the source snapshot.
- One primary key_message per slide.
- Never invent prices, warranties, SLAs, or dates.
- Missing information => review_required=true and/or assumptions.
- Include speaker_notes for every slide.
- Prefer REVIEW REQUIRED over guessing.

Return JSON only:
{
  "title": string,
  "slides": [
    {
      "section_type": string,
      "title": string,
      "sequence": number,
      "objective": string,
      "key_message": string,
      "body_content": string,
      "visual_type": "none|bullets|architecture|components|callout",
      "visual_data": {},
      "speaker_notes": string,
      "confidence": number,
      "review_required": boolean,
      "assumptions": [string],
      "source_refs": [{"ref_kind": string, "ref_id": string|null, "label": string}]
    }
  ]
}

Follow the content plan slide order exactly.
